import WaitingScreen from '../../../live-quiz/ui/WaitingScreen.jsx';

export default function WorkSituationView() {
  return (
    <WaitingScreen title="Situação Profissional">
      Este minigame já está modelado para o match online, mas ainda não entra na rotação da Fase 1.
    </WaitingScreen>
  );
}

